{% extends 'base.html.twig' %}

{% block title %}{{ parent()}} | Détails {% endblock %}

{% block body %}

    <div class="container">
        <h1 id="titrePage">Détails souhait</h1>
        <p>{{ wish1.title }}</p>
        <p>{{ wish1.description }}</p>
        <p>{{ wish1.author }}</p>
        <p>{{ wish1.dateCreated|date('d/m/Y') }}</p>
    </div>

{% endblock %}